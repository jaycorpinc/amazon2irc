require "spec_helper"

RSpec.describe Amazon2irc do
  it "has a version number" do
    expect(Amazon2irc::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
